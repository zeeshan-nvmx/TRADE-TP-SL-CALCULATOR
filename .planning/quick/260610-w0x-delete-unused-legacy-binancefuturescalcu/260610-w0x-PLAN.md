---
phase: quick-260610-w0x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BinanceFuturesCalculator.jsx
autonomous: true
requirements:
  - DELETE-LEGACY-MONOLITH
must_haves:
  truths:
    - "src/BinanceFuturesCalculator.jsx no longer exists in the repository"
    - "npm run build completes without errors"
    - "The app entry point (src/App.jsx) still renders correctly via BinanceFuturesCalculatorNew"
  artifacts:
    - path: "src/BinanceFuturesCalculator.jsx"
      provides: "File must NOT exist after task completes"
  key_links:
    - from: "src/App.jsx"
      to: "src/BinanceFuturesCalculatorNew.jsx"
      via: "import statement"
      pattern: "BinanceFuturesCalculatorNew"
---

<objective>
Delete the unused 1642-line legacy monolith src/BinanceFuturesCalculator.jsx from the repository. The app has been fully migrated to the modular BinanceFuturesCalculatorNew architecture; the old file is an orphan that adds maintenance confusion without providing any value.

Purpose: Reduce codebase surface area and eliminate the risk of accidental edits to dead code.
Output: File removed from git history, build passing, commit recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove file, verify build, commit</name>
  <files>src/BinanceFuturesCalculator.jsx</files>
  <action>
    Run the following commands in sequence:

    1. Confirm nothing imports the file (safety check before deletion):
       grep -r "BinanceFuturesCalculator" src/ --include="*.jsx" --include="*.js" -l

       Expected output: only src/BinanceFuturesCalculator.jsx itself (its own definition). If any OTHER file is listed, stop and report — do not delete.

    2. Remove the file from git tracking:
       git rm src/BinanceFuturesCalculator.jsx

    3. Verify the production build still passes (no import errors):
       npm run build

       The build must exit 0. If it fails, restore the file with `git checkout HEAD -- src/BinanceFuturesCalculator.jsx` and report the error.

    4. Commit the removal:
       git commit -m "chore: remove unused legacy BinanceFuturesCalculator.jsx monolith

    File was superseded by the modular BinanceFuturesCalculatorNew architecture.
    App.jsx renders BinanceFuturesCalculatorNew; nothing imports the old file.

    Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
  </action>
  <verify>
    <automated>
      test ! -f src/BinanceFuturesCalculator.jsx && echo "FILE_GONE" || echo "FILE_STILL_EXISTS"
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    src/BinanceFuturesCalculator.jsx does not exist in the working tree, `npm run build` exits 0, and the removal is recorded in git history.
  </done>
</task>

</tasks>

<threat_model>
No trust boundaries introduced or modified. This is a dead-code deletion with no runtime impact.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w0x-01 | Tampering | git rm on wrong file | accept | Safety grep in step 1 confirms no other file imports BinanceFuturesCalculator before deletion proceeds |
</threat_model>

<verification>
- `test ! -f src/BinanceFuturesCalculator.jsx` exits 0
- `npm run build` exits 0 with no import-resolution errors
- `git log --oneline -1` shows the chore commit
</verification>

<success_criteria>
Legacy monolith deleted, production build green, commit present in git log.
</success_criteria>

<output>
Create `.planning/quick/260610-w0x-delete-unused-legacy-binancefuturescalcu/260610-w0x-01-SUMMARY.md` when done.
</output>
