---
phase: 01-calculator-accuracy-ux-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/hooks/useCalculator.js
autonomous: true
requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04
  - PRICE-05

must_haves:
  truths:
    - "useCalculator hook exports tpAutoPriceUpdate ([true, false, false]) and slAutoPriceUpdate (false) state"
    - "useCalculator hook exports handleTPAutoPriceToggle(index, enabled) and handleSLAutoPriceToggle(enabled) handlers"
    - "autoPriceUpdate defaults to false (entry price manual on load)"
    - "tpAutoPriceUpdate[0] defaults to true (TP1 auto on load)"
    - "tpAutoPriceUpdate[1] and tpAutoPriceUpdate[2] default to false (TP2/TP3 manual on load)"
  artifacts:
    - path: "src/hooks/useCalculator.js"
      provides: "tpAutoPriceUpdate state, slAutoPriceUpdate state, handleTPAutoPriceToggle, handleSLAutoPriceToggle handlers — all exported in return object"
  key_links:
    - from: "src/hooks/useCalculator.js autoPriceUpdate state"
      to: "useState(false)"
      via: "state initialization"
      pattern: "autoPriceUpdate.*useState\\(false\\)"
    - from: "src/hooks/useCalculator.js tpAutoPriceUpdate"
      to: "useState([true, false, false])"
      via: "state initialization"
      pattern: "tpAutoPriceUpdate.*useState"
---

<objective>
Extend useCalculator hook with per-field auto-price state for TP1/TP2/TP3 and SL, fix the entry price default (true → false), and export all new state and handlers.

Purpose: Plan 03 (UI toggles) and BinanceFuturesCalculatorNew orchestration depend on these state variables and handlers being available from the hook. This plan creates the contracts that the UI consumes.
Output: useCalculator.js with new state variables (tpAutoPriceUpdate, slAutoPriceUpdate), fixed autoPriceUpdate default, two new handler functions, and all six new exports added to the return object.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-calculator-accuracy-ux-fixes/01-RESEARCH.md
@.planning/phases/01-calculator-accuracy-ux-fixes/01-PATTERNS.md
@.planning/phases/01-calculator-accuracy-ux-fixes/01-01-SUMMARY.md
</context>

<interfaces>
From src/hooks/useCalculator.js (current return object, lines 715-740):
  setAutoPriceUpdate       — existing setter for entry price toggle
  handleEntryPriceChange   — existing handler for entry price field
  handleTakeProfitChange   — existing handler: (index, field, value)
  handleStopLossPriceChange — existing handler for SL price field

Pattern from existing array state mutation (see TP quantity redistribution ~line 337):
  const updated = [...array]
  updated[index] = newValue
  setState(updated)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix autoPriceUpdate default and add tpAutoPriceUpdate / slAutoPriceUpdate state</name>
  <files>src/hooks/useCalculator.js</files>
  <read_first>
    - src/hooks/useCalculator.js — read lines 1-50 to see current state declarations, specifically line 18 (autoPriceUpdate) and lines 44-48 (calculationMode, positionSizeUSDT area) to find correct insertion point
  </read_first>
  <action>
    Three changes to src/hooks/useCalculator.js in the state declarations section:

    1. On line 18, change the autoPriceUpdate initialization:
         FROM: useState(true)
         TO:   useState(false)
       This fixes PRICE-03 — entry price now defaults to manual on app load.

    2. Immediately after line 18 (the autoPriceUpdate line), add two new state declarations:
         const [tpAutoPriceUpdate, setTPAutoPriceUpdate] = useState([true, false, false])
         const [slAutoPriceUpdate, setSLAutoPriceUpdate] = useState(false)
       First array element (index 0) = TP1 defaults to true (PRICE-04).
       Elements 1 and 2 = TP2/TP3 default to false (PRICE-04).
       slAutoPriceUpdate = false (PRICE-05).

    Do not add these states to the takeProfits array object — they must remain as separate top-level state variables per the anti-pattern warning in RESEARCH.md.
  </action>
  <verify>
    <automated>grep -n "autoPriceUpdate\|tpAutoPriceUpdate\|slAutoPriceUpdate" src/hooks/useCalculator.js | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Line 18 of useCalculator.js reads: `const [autoPriceUpdate, setAutoPriceUpdate] = useState(false)`
    - Lines immediately after contain: `const [tpAutoPriceUpdate, setTPAutoPriceUpdate] = useState([true, false, false])`
    - Lines immediately after contain: `const [slAutoPriceUpdate, setSLAutoPriceUpdate] = useState(false)`
    - grep output shows autoPriceUpdate initialized to false (not true)
    - grep output shows tpAutoPriceUpdate initialized with array containing true as first element
    - The takeProfits DEFAULT_TAKE_PROFITS constant in presets.js is NOT modified
  </acceptance_criteria>
  <done>autoPriceUpdate defaults to false; tpAutoPriceUpdate and slAutoPriceUpdate state declared with correct defaults</done>
</task>

<task type="auto">
  <name>Task 2: Add handleTPAutoPriceToggle and handleSLAutoPriceToggle handlers, export all new state and handlers</name>
  <files>src/hooks/useCalculator.js</files>
  <read_first>
    - src/hooks/useCalculator.js — read lines 80-130 to see the input handlers section (handleAccountSizeChange, handleLeverageChange, handleEntryPriceChange) to identify the insertion point for new handlers
    - src/hooks/useCalculator.js — read lines 710-741 to see the full return object to identify where to add new exports
  </read_first>
  <action>
    Two additions to src/hooks/useCalculator.js:

    1. In the input handlers section (after the existing handlers, around line 98-110), add two new handler functions:

       handleTPAutoPriceToggle(index, enabled): creates a shallow copy of tpAutoPriceUpdate, sets copy[index] = enabled, calls setTPAutoPriceUpdate with the new array. Follows the array mutation pattern from line ~337 (TP quantity redistribution).

       handleSLAutoPriceToggle(enabled): calls setSLAutoPriceUpdate(enabled) directly. Single boolean, no array mutation needed.

    2. In the return object (after the existing `setAutoPriceUpdate` export line ~722), add these six new exports:
         tpAutoPriceUpdate,
         slAutoPriceUpdate,
         setTPAutoPriceUpdate,
         setSLAutoPriceUpdate,
         handleTPAutoPriceToggle,
         handleSLAutoPriceToggle,

    Do not modify the calculateAll() dependency array or any useEffect hooks — the new toggle states do not affect P&L calculations and must not be added to the calculation dependency array.
  </action>
  <verify>
    <automated>grep -n "handleTPAutoPriceToggle\|handleSLAutoPriceToggle\|tpAutoPriceUpdate\|slAutoPriceUpdate" src/hooks/useCalculator.js</automated>
  </verify>
  <acceptance_criteria>
    - grep output shows handleTPAutoPriceToggle defined as a function (not just exported)
    - grep output shows handleSLAutoPriceToggle defined as a function (not just exported)
    - Both handlers appear in the return object section (lines after the existing setAutoPriceUpdate export)
    - tpAutoPriceUpdate and slAutoPriceUpdate appear in the return object
    - setTPAutoPriceUpdate and setSLAutoPriceUpdate appear in the return object
    - npm run lint passes with no new errors (hooks rules not violated)
    - The calculateAll() useEffect dependency array does not include tpAutoPriceUpdate or slAutoPriceUpdate
  </acceptance_criteria>
  <done>Both toggle handlers defined and exported; all six new return fields present in useCalculator return object</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook state → Component | Toggle state flows down as props; no external input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tpAutoPriceUpdate state | accept | Client-side React state; no serialization, no persistence, no external write path. Toggled only by button click handler. |
| T-02-02 | Denial of Service | calculateAll() effect | mitigate | Do NOT add tpAutoPriceUpdate/slAutoPriceUpdate to calculateAll dependency array — toggle changes must not trigger P&L recalculation loop. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages. All changes are pure React state hooks using existing useState import. |
</threat_model>

<verification>
After `npm run dev`:
1. Open browser dev tools Console tab — no React hooks errors or infinite re-render warnings
2. Confirm lint passes: `npm run lint` — zero errors
3. App loads and calculator renders without white screen
4. (Full toggle UI verification deferred to Plan 03 — this plan only creates the state layer)
</verification>

<success_criteria>
- autoPriceUpdate initialized to false in useCalculator.js
- tpAutoPriceUpdate initialized to [true, false, false]
- slAutoPriceUpdate initialized to false
- handleTPAutoPriceToggle(index, enabled) defined and exported
- handleSLAutoPriceToggle(enabled) defined and exported
- All six new fields present in the return object
- npm run lint passes
- calculateAll() effect dependency array unchanged
</success_criteria>

<output>
Create `.planning/phases/01-calculator-accuracy-ux-fixes/01-02-SUMMARY.md` when done
</output>
