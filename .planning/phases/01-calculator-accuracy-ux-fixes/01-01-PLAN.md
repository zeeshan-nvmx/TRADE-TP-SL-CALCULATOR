---
phase: 01-calculator-accuracy-ux-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCalculator.js
  - src/constants/presets.js
autonomous: true
requirements:
  - FEE-01
  - FEE-02
  - POS-01
  - POS-02

must_haves:
  truths:
    - "Net P&L for a TP exit is lower than gross P&L by the taker fee amount (not maker) for both Binance and Bybit"
    - "Fixed USDT preset buttons start with 20 then 50 before the existing 100 value"
    - "Default margin input shows 50 USDT on app load (not 100)"
  artifacts:
    - path: "src/hooks/useCalculator.js"
      provides: "tpFeeRate uses fees.taker on line 444; positionSizeUSDTInput initialized to '50' on line 45; positionSizeUSDT initialized to 50 on line 46"
    - path: "src/constants/presets.js"
      provides: "FIXED_USDT_PRESETS array starts with [20, 50, 100, ...]"
  key_links:
    - from: "src/hooks/useCalculator.js line 444"
      to: "EXCHANGE_FEES[exchange].taker"
      via: "fees.taker assignment"
      pattern: "tpFeeRate = fees\\.taker"
    - from: "src/constants/presets.js FIXED_USDT_PRESETS"
      to: "src/components/PositionSizing.jsx preset buttons"
      via: "import and map"
      pattern: "FIXED_USDT_PRESETS"
---

<objective>
Fix the TP exit fee calculation bug (fees.maker → fees.taker) and update position sizing defaults (preset array + initial state).

Purpose: The TP fee bug causes net P&L to be overstated by the difference between maker and taker rates (0.02% on Binance, 0.05% on Bybit). Fixing it makes the calculator accurate for real market-order exits. Position sizing defaults support micro-trading scenarios.
Output: useCalculator.js with correct tpFeeRate and 50 USDT defaults; presets.js with [20, 50, ...] FIXED_USDT_PRESETS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-calculator-accuracy-ux-fixes/01-RESEARCH.md
@.planning/phases/01-calculator-accuracy-ux-fixes/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TP exit fee rate (FEE-01) and verify exchange fee constants (FEE-02)</name>
  <files>src/hooks/useCalculator.js</files>
  <read_first>
    - src/hooks/useCalculator.js — read lines 440-450 to see the fee assignment block before touching it
    - src/constants/presets.js — read EXCHANGE_FEES to confirm taker values are 0.0004 (Binance) and 0.0006 (Bybit)
  </read_first>
  <action>
    In src/hooks/useCalculator.js at the fee assignment block (around line 444), change the single line:
      const tpFeeRate = fees.maker
    to:
      const tpFeeRate = fees.taker

    No other changes in this task. Do not modify slFeeRate, trailingSlFeeRate, or entryFeeRate — they are already correct.

    In src/constants/presets.js, read EXCHANGE_FEES and confirm:
      binance taker = 0.0004 (0.04%)
      bybit taker = 0.0006 (0.06%)
    These values are already correct per FEE-02. No change needed — record the verification in the task summary only.
  </action>
  <verify>
    <automated>grep -n "tpFeeRate" src/hooks/useCalculator.js</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useCalculator.js line ~444 reads: `const tpFeeRate = fees.taker` (not fees.maker)
    - grep output shows exactly one match for `tpFeeRate = fees.taker` and zero matches for `tpFeeRate = fees.maker`
    - src/constants/presets.js EXCHANGE_FEES contains `taker: 0.0004` for binance and `taker: 0.0006` for bybit (unchanged, verified)
  </acceptance_criteria>
  <done>tpFeeRate uses fees.taker; fee constants confirmed correct for 2026</done>
</task>

<task type="auto">
  <name>Task 2: Update position sizing defaults and presets (POS-01, POS-02)</name>
  <files>src/hooks/useCalculator.js, src/constants/presets.js</files>
  <read_first>
    - src/hooks/useCalculator.js — read lines 44-47 to see positionSizeUSDTInput and positionSizeUSDT useState declarations
    - src/constants/presets.js — read lines 18-21 to see current FIXED_USDT_PRESETS array
  </read_first>
  <action>
    In src/hooks/useCalculator.js, update the two useState declarations for position size (around lines 45-46):
      Change: useState('100') to useState('50') for positionSizeUSDTInput
      Change: useState(100) to useState(50) for positionSizeUSDT

    In src/constants/presets.js, update the FIXED_USDT_PRESETS export to prepend 20 and 50:
      The array must begin with [20, 50, 100, 200, 250, 300, 400, 500, 600, 800, 1000, 1500, 2000, 2500, 3000, 3500, 4000, 4500, 5000, 6000, 7500, 8000, 10000, 12500, 15000, 20000]
      All existing values after 50 must remain unchanged and in the same order.
  </action>
  <verify>
    <automated>grep -n "positionSizeUSDT" src/hooks/useCalculator.js | grep "useState"</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useCalculator.js: `useState('50')` on the positionSizeUSDTInput line and `useState(50)` on the positionSizeUSDT line
    - src/constants/presets.js: FIXED_USDT_PRESETS first two values are 20 and 50 (confirmed by reading the array)
    - grep output for FIXED_USDT_PRESETS in presets.js shows the array starting with 20 on first element
    - npm run lint passes with no new errors
  </acceptance_criteria>
  <done>positionSizeUSDTInput defaults to '50'; positionSizeUSDT defaults to 50; FIXED_USDT_PRESETS starts with [20, 50, 100, ...]</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → Build output | No runtime boundary — all changes are compile-time constants and state initializers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | presets.js EXCHANGE_FEES constants | accept | Read-only constants; no user-modifiable path. Fee rates sourced from official exchange docs and hard-coded. |
| T-01-02 | Information Disclosure | useCalculator.js fee calculation | accept | Client-side calculator; no sensitive data in fee rates. All values are public exchange rates. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan. All changes use existing React, Vite, Tailwind stack. |
</threat_model>

<verification>
Manual verification after `npm run dev`:
1. Open app in browser
2. Set Binance, 1000 USDT position at 50000 BTC, 1x leverage, TP1 at 51000 (2%)
3. Gross TP1 profit = 20 USDT, Entry fee = 1000 * 0.0004 = 0.4 USDT, Exit fee = 1000 * 0.0004 = 0.4 USDT → Net = 19.2 USDT
4. If ResultsPanel shows 19.2 (not 19.8 which was the old maker-fee result), FEE-01 is fixed
5. Open PositionSizing section — margin input shows "50" on first load
6. Preset buttons show "20" and "50" before "100"
</verification>

<success_criteria>
- TP net profit uses taker fee (not maker) for Binance and Bybit
- EXCHANGE_FEES constants verified correct for 2026: Binance taker 0.0004, Bybit taker 0.0006
- Fixed USDT preset buttons: first button is 20, second is 50, third is 100
- Margin input field shows "50" on page load (not "100")
- npm run lint passes
</success_criteria>

<output>
Create `.planning/phases/01-calculator-accuracy-ux-fixes/01-01-SUMMARY.md` when done
</output>
